CC = g++
STANDARD = -std=c++14
CFLAGS = -Wall $(STANDARD) 
DFLAGS = $(CFLAGS) $(STANDARD) -g -pg
 
SRC := $(filter-out test.cpp, $(wildcard *.cpp)) # grab anything with a .c ending

USER_FLAGS := $(vars)
UNAME := $(shell uname)

debug: pre
	@$(CC) $(SRC) $(DFLAGS) -o 'db.out'
	@echo "Debug build completed."

all: pre
	@$(CC) $(SRC) $(CFLAGS) -O3 -march=native -o 'prod.out' 
	@echo "Optimized build completed."

pre:
	@echo "Building on: $(UNAME), user params: $(USER_FLAGS)"

clean:
	@rm -f *.out
	@rm -f *.s *.o
	
	@rm -rf *.out.dSYM # get rid of debug info, macOS specific
	@rm -f *.gch
