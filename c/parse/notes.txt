lexer -> parser -> semantic analyzer -> optimizer -> code generator

lexer: (flex)
	characters -> tokens
	2 -> number, '=' -> equals operator
	
parser: (bison)
	tokens -> AST
	I think this is a bit harder...

semantic analyzer: (--)
	verifies the code
		type checking
		object binding

code generator: (there might be something for this)
	AST -> machine code
	the fun stuff
