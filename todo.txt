fix trackpad
	workaround: use a mouse all the time
fix firefox and chrome getting hot all the time
	default fan curves are way too high
	fix with adding "applesmc" and something else to a file in /etc?
fix netflix not playing
	sudo apt-get install libavcodec-extra
	I think this fixed it in firefox... quality is ok
	Using chromium also fixes this problem
fix sleep

fix kworker acpi threads hitting 100% cpu usage

