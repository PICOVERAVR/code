x64 stack instructions

call <addr> = push %rip+8, jmp <addr>
ret <num> = (%rsp)->%rip, pop <num> bytes off stack

loop <addr> = dec %rcx, jnz <addr>

enter <num> = create stack frame with <num> bytes of space
leave = destroy stack frame

lock?
