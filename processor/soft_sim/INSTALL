As per most autotools software, run:

$ ./configure && make

(the executable will be located in the 'src' directory)
To test the simulator, run:

$ make check

To install the simulator, run:

$ make install

