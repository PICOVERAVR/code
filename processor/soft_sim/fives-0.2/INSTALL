installation is pretty simple:
	$ ./configure && make
	$ sudo make install # if you want it installed

packages required:
	check: unit testing framework, only required for testing

how to run stuff:
	executable is located in src directory
	run with ./src/proc <binary file>

testing:
	$ make check

